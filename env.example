# 服务认证 Token（ShowDoc 调用 AI 服务时使用）
SERVICE_TOKEN=your-secret-token-here

# 向量数据库配置
QDRANT_COLLECTION_PREFIX=showdoc_item_

# 日志级别
LOG_LEVEL=WARNING

# API 服务 Uvicorn workers 数量（控制并发处理能力，默认 1）
# 建议根据服务器 CPU 核心数设置，通常为 CPU 核心数或 2-4 倍
WORKERS=1

# Worker 服务 Celery 并发数（同时处理的任务数，默认 4）
# 支持多用户并发索引，空闲时自动释放内存
# 建议根据服务器内存和任务量设置，通常为 2-6
WORKER_CONCURRENCY=4

