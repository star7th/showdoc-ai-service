# Web 框架
fastapi==0.104.1
uvicorn==0.24.0  # 移除 [standard] 以减少内存占用
python-multipart==0.0.6

# 向量数据库
qdrant-client>=1.7.0

# 任务队列
celery==5.3.4
redis==5.0.1
gevent==23.9.1  # Celery gevent pool 支持（协程池，支持并发且节省内存）

# LLM 调用
openai==1.3.5
dashscope==1.17.0  # 通义千问
httpx==0.25.1

# 工具库
pydantic==2.5.0
pydantic-settings==2.1.0
python-dotenv==1.0.0
pyyaml==6.0.1

# 以下依赖已移除（未使用，可节省 80-100MB 内存）：
# langchain==0.0.350  # 未使用
# langchain-community==0.0.10  # 未使用
# jieba==0.42.1  # 未使用
# markdown==3.5.1  # 未使用
# markdown-it-py==3.0.0  # 未使用
# aiofiles==23.2.1  # 未使用
# python-jose[cryptography]==3.3.0  # 未使用
# passlib[bcrypt]==1.7.4  # 未使用
# psutil==5.9.6  # 可选监控工具，已移除
